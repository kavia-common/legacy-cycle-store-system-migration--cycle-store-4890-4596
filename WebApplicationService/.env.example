# Base URL of API Gateway (e.g., http://localhost:3000 in dev proxy or actual gateway)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
# Optional analytics token to identify the app when reporting metrics/logs
NEXT_PUBLIC_ANALYTICS_TOKEN=webapp-dev
